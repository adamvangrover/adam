# Core Data & ML
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
statsmodels==0.14.2 # Note: statsmodels was not in pip freeze, assuming environment default or needs install if critical.
# torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu # PyTorch was not in pip freeze, commented out for safety or should be installed explicitly if needed.
# torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cpu
# torchaudio==2.3.0 --index-url https://download.pytorch.org/whl/cpu
# xgboost==1.7.5 # Not in pip freeze

# Knowledge Graph & Agents
langchain==1.1.2
langchain-community==0.4.1
langchain-core==1.1.1
langgraph==1.0.4
langgraph-checkpoint==3.0.1
langgraph-prebuilt==1.0.5
langgraph-sdk==0.2.12
semantic-kernel==1.39.0
networkx==3.6
# rdflib==6.3.2 # Not in pip freeze

# NLP & Text Processing
transformers==4.57.3
spacy==3.8.11
spacy-legacy==3.0.12
spacy-loggers==1.0.5
textblob==0.19.0
tiktoken==0.12.0
nltk==3.9.2
beautifulsoup4==4.14.3
regex==2025.11.3

# Data Sources & Web
requests==2.32.5
aiohttp==3.13.2
tweepy==4.16.0
# feedparser==6.0.10 # Not in pip freeze
# scrapy==2.8.0 # Not in pip freeze

# Infrastructure & Utils
pydantic==2.11.10
pydantic-settings==2.12.0
pydantic_core==2.33.2
python-dotenv==1.2.1
PyYAML==6.0.3
pika==1.3.2
redis==7.1.0

# Visualization
# matplotlib==3.7.1 # Not in pip freeze
# seaborn==0.12.2 # Not in pip freeze

# Web App
# Flask==3.0.0 # Not in pip freeze
# Flask-SocketIO==5.3.6 # Not in pip freeze
# Flask-SQLAlchemy==3.1.1 # Not in pip freeze
# Flask-JWT-Extended==4.6.0 # Not in pip freeze
# Celery==5.3.6 # Not in pip freeze
# Flask-Cors # Not in pip freeze

# Reporting
# reportlab==4.0.9 # Not in pip freeze
# fpdf==1.7.2 # Not in pip freeze

# Cloud
# boto3==1.26.134 # Not in pip freeze
# google-cloud-storage==2.8.0 # Not in pip freeze

# Other Deps from Freeze
playwright==1.55.0
SQLAlchemy==2.0.44
openai==1.109.1
azure-ai-agents==1.2.0b6
