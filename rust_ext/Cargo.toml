[package]
name = "adam-kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "adam_kernel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.0", features = ["abi3-py310", "extension-module"] }

[features]
default = []
