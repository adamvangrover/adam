# Persona
Act as a Senior Data Scientist.

# Task
Write a Python script using pandas to automate the cleaning of a messy dataset. Ensure the code is wrapped in a reusable class or function.

# Context
We have a messy dataset that needs standard cleaning procedures before analysis. The cleaning must include:
1. Handling missing values (impute numeric, drop or fill categorical).
2. Removing duplicate rows.
3. Detecting and handling outliers in continuous variables using the IQR method.
4. Normalizing the column names (snake_case).

# Format
Provide the complete Python script with clear comments. Ensure it is wrapped in a reusable class or function. Output only the code block.
