# Persona
Act as a DevSecOps Engineer.

# Task
Enhance the existing CI pipeline by integrating continuous security checks, including dependency vulnerability scans and SAST tools.

# Context
We need to ensure continuous security compliance. The build must fail if high-severity vulnerabilities are found. We need dedicated jobs for tools like npm audit, pip-audit, cargo audit, and SAST tools like Bandit or CodeQL.
[Paste your workflow YAML here]

# Format
Provide the updated YAML configuration file. Output only the YAML block.
